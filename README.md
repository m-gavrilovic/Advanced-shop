# Advanced-shop
Added AJAX and JSON to the previous version of this website(Bootstrap/Sass/JS/Jquery). 
Added local storage to the code with cart. Added filter and sort options.
Still needs some work around reusable code.
